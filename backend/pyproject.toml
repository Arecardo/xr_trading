[project]
name = "xr-trading-backend"
version = "0.1.0"
description = "XR-Trading core business backend: portfolios, accounts, strategies, risk, execution, valuation."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1.0",
    "pydantic>=2.9,<3.0",
    "httpx>=0.27,<1.0",
    "uvicorn[standard]>=0.30,<1.0",
    "sqlalchemy>=2.0,<3.0",
    "alembic>=1.13,<2.0",
    "psycopg[binary]>=3.1,<4.0",
    "asyncpg>=0.29,<1.0",
    "greenlet>=3.0,<4.0",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.6,<1.0",
    "mypy>=1.11,<2.0",
    "pytest>=8.3,<9.0",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = [
    "api*",
    "application*",
    "domain*",
    "adapters*",
    "repository*",
    "config*",
    "observability*",
    "backtest*",
]
exclude = ["tests*"]

# --- ruff: lint + format (python-backend-standards.md §4, §10) ---
[tool.ruff]
line-length = 100
target-version = "py311"
# app.py is the legacy stdlib http.server + SQLite prototype, kept untouched
# as historical reference (python-backend-standards.md §3) -- it predates
# this tooling and is intentionally excluded from lint/format enforcement.
extend-exclude = ["app.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "N", "C4"]

[tool.ruff.lint.isort]
known-first-party = [
    "api",
    "application",
    "domain",
    "adapters",
    "repository",
    "config",
    "observability",
    "backtest",
]

[tool.ruff.format]
quote-style = "double"

# --- mypy: full type annotations required, core domain must pass (§4, §10) ---
[tool.mypy]
python_version = "3.11"
# No `tests/**/__init__.py` files exist (by convention -- pytest doesn't
# need them here), which makes mypy's default file->module mapping collide
# once more than one test subpackage has a same-named file (e.g. two
# `conftest.py`s under different tests/ subdirectories, introduced once
# BE-002 and BT-001 both added integration-test conftest.py files).
# explicit_package_bases (mypy's own suggested fix for "Duplicate module")
# qualifies each module by its path from the nearest dir without
# __init__.py instead of by basename alone.
explicit_package_bases = true
mypy_path = "."
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
no_implicit_optional = true
strict_equality = true
# app.py is the legacy stdlib http.server + SQLite prototype, kept untouched
# as historical reference (python-backend-standards.md §3); it predates
# this tooling and is intentionally excluded from type-checking.
exclude = ["^app\\.py$"]

# --- pytest (§9, §10) ---
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
