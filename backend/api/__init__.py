"""HTTP routing, request parsing, response encoding.

Only ``api/main.py`` (the FastAPI app + ``/healthz``) exists so far. Real
business endpoints follow once ``application``/``domain`` have something to
orchestrate (BE-002/BE-003).
"""
