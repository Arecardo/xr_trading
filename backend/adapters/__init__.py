"""Outbound adapters to external systems (market data providers, brokers).

Intentionally empty scaffolding for BE-001. ``adapters/market_data`` (client
for ``market-info-service``) is BE-004 scope; ``adapters/brokers`` (Longbridge/
Bybit trading adapters, kept separate from market-data adapters per
project-conventions.md §2) is later scope.
"""
